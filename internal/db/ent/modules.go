// Code generated by ent, DO NOT EDIT.

package ent

import (
	"ItsBagelBot/internal/db/ent/modules"
	"ItsBagelBot/internal/db/ent/user"
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Modules is the model entity for the Modules schema.
type Modules struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// IsEnabled holds the value of the "is_enabled" field.
	IsEnabled bool `json:"is_enabled,omitempty"`
	// Configs holds the value of the "configs" field.
	Configs []uint8 `json:"configs,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ModulesQuery when eager-loading is set.
	Edges        ModulesEdges `json:"edges"`
	user_modules *uint64
	selectValues sql.SelectValues
}

// ModulesEdges holds the relations/edges for other nodes in the graph.
type ModulesEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ModulesEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Modules) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case modules.FieldConfigs:
			values[i] = new([]byte)
		case modules.FieldIsEnabled:
			values[i] = new(sql.NullBool)
		case modules.FieldID:
			values[i] = new(sql.NullInt64)
		case modules.FieldName:
			values[i] = new(sql.NullString)
		case modules.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case modules.ForeignKeys[0]: // user_modules
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Modules fields.
func (_m *Modules) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case modules.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case modules.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case modules.FieldIsEnabled:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_enabled", values[i])
			} else if value.Valid {
				_m.IsEnabled = value.Bool
			}
		case modules.FieldConfigs:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field configs", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Configs); err != nil {
					return fmt.Errorf("unmarshal field configs: %w", err)
				}
			}
		case modules.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case modules.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_modules", value)
			} else if value.Valid {
				_m.user_modules = new(uint64)
				*_m.user_modules = uint64(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Modules.
// This includes values selected through modifiers, order, etc.
func (_m *Modules) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the Modules entity.
func (_m *Modules) QueryUser() *UserQuery {
	return NewModulesClient(_m.config).QueryUser(_m)
}

// Update returns a builder for updating this Modules.
// Note that you need to call Modules.Unwrap() before calling this method if this Modules
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Modules) Update() *ModulesUpdateOne {
	return NewModulesClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Modules entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Modules) Unwrap() *Modules {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Modules is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Modules) String() string {
	var builder strings.Builder
	builder.WriteString("Modules(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("is_enabled=")
	builder.WriteString(fmt.Sprintf("%v", _m.IsEnabled))
	builder.WriteString(", ")
	builder.WriteString("configs=")
	builder.WriteString(fmt.Sprintf("%v", _m.Configs))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ModulesSlice is a parsable slice of Modules.
type ModulesSlice []*Modules
