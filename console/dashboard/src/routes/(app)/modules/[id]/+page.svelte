<script lang="ts">
  // This route always redirects to /modules in load (the per-module detail page
  // was folded into the deck's docked inspector), so this component never
  // renders. It exists only to satisfy the route's page requirement.
</script>
