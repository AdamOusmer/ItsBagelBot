# Local-only test fixture: never bind this plaintext listener beyond loopback.
port: 14222
host: "127.0.0.1"
server_name: "nats-auth-smoke"

jetstream {
  domain: hub
  store_dir: "/tmp/itsbagelbot-nats-auth-smoke"
  max_mem: 128MB
  max_file: 128MB
}

include "nats-auth.conf"
