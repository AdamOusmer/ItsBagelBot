# worker1 is a latency-sensitive NATS voter, not a battery-powered client.
# These options take effect on the next module load/reboot; do not reload the
# Wi-Fi driver while production workloads are using this node.
options rtw88_core disable_lps_deep=Y
options rtw88_pci disable_aspm=Y
