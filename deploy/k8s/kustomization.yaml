apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: production
resources:
  - admin.yaml
  - commands.yaml
  - console-dashboard.yaml
  # console-admin.yaml held out until its cross-domain auth is finished; it
  # shares the admin.tail451e6d.ts.net host with the still-running Go admin.
  - modules.yaml
  - outgress.yaml
  - projector.yaml
  - transactions.yaml
  - users.yaml
  - twitch-ingress.yaml
  - network-policies.yaml
  - linkerd-auth.yaml
