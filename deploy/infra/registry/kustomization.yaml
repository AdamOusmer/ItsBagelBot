apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # GHCR pull-through cache. Deliberately NOT part of the `cluster`
  # Kustomization: it owns a pinned ClusterIP that agents reference literally in
  # registries.yaml, so it gets its own Kustomization and its own blast radius.
  - registry.yaml
