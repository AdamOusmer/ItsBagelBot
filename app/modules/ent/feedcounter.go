// Code generated by ent, DO NOT EDIT.

package ent

import (
	"ItsBagelBot/app/modules/ent/feedcounter"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// FeedCounter is the model entity for the FeedCounter schema.
type FeedCounter struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Count holds the value of the "count" field.
	Count        uint64 `json:"count,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*FeedCounter) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case feedcounter.FieldID, feedcounter.FieldCount:
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the FeedCounter fields.
func (_m *FeedCounter) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case feedcounter.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case feedcounter.FieldCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field count", values[i])
			} else if value.Valid {
				_m.Count = uint64(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the FeedCounter.
// This includes values selected through modifiers, order, etc.
func (_m *FeedCounter) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this FeedCounter.
// Note that you need to call FeedCounter.Unwrap() before calling this method if this FeedCounter
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *FeedCounter) Update() *FeedCounterUpdateOne {
	return NewFeedCounterClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the FeedCounter entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *FeedCounter) Unwrap() *FeedCounter {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: FeedCounter is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *FeedCounter) String() string {
	var builder strings.Builder
	builder.WriteString("FeedCounter(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("count=")
	builder.WriteString(fmt.Sprintf("%v", _m.Count))
	builder.WriteByte(')')
	return builder.String()
}

// FeedCounters is a parsable slice of FeedCounter.
type FeedCounters []*FeedCounter
