// Code generated by ent, DO NOT EDIT.

package ent

import (
	"ItsBagelBot/app/db/transactions/ent/giveawayfulfillmentplan"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// GiveawayFulfillmentPlan is the model entity for the GiveawayFulfillmentPlan schema.
type GiveawayFulfillmentPlan struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// AwardID holds the value of the "award_id" field.
	AwardID string `json:"award_id,omitempty"`
	// IntervalRule holds the value of the "interval_rule" field.
	IntervalRule string `json:"interval_rule,omitempty"`
	// StartAt holds the value of the "start_at" field.
	StartAt time.Time `json:"start_at,omitempty"`
	// EndAt holds the value of the "end_at" field.
	EndAt time.Time `json:"end_at,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*GiveawayFulfillmentPlan) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case giveawayfulfillmentplan.FieldID, giveawayfulfillmentplan.FieldAwardID, giveawayfulfillmentplan.FieldIntervalRule:
			values[i] = new(sql.NullString)
		case giveawayfulfillmentplan.FieldStartAt, giveawayfulfillmentplan.FieldEndAt, giveawayfulfillmentplan.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the GiveawayFulfillmentPlan fields.
func (_m *GiveawayFulfillmentPlan) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case giveawayfulfillmentplan.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case giveawayfulfillmentplan.FieldAwardID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field award_id", values[i])
			} else if value.Valid {
				_m.AwardID = value.String
			}
		case giveawayfulfillmentplan.FieldIntervalRule:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field interval_rule", values[i])
			} else if value.Valid {
				_m.IntervalRule = value.String
			}
		case giveawayfulfillmentplan.FieldStartAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field start_at", values[i])
			} else if value.Valid {
				_m.StartAt = value.Time
			}
		case giveawayfulfillmentplan.FieldEndAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field end_at", values[i])
			} else if value.Valid {
				_m.EndAt = value.Time
			}
		case giveawayfulfillmentplan.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the GiveawayFulfillmentPlan.
// This includes values selected through modifiers, order, etc.
func (_m *GiveawayFulfillmentPlan) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this GiveawayFulfillmentPlan.
// Note that you need to call GiveawayFulfillmentPlan.Unwrap() before calling this method if this GiveawayFulfillmentPlan
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *GiveawayFulfillmentPlan) Update() *GiveawayFulfillmentPlanUpdateOne {
	return NewGiveawayFulfillmentPlanClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the GiveawayFulfillmentPlan entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *GiveawayFulfillmentPlan) Unwrap() *GiveawayFulfillmentPlan {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: GiveawayFulfillmentPlan is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *GiveawayFulfillmentPlan) String() string {
	var builder strings.Builder
	builder.WriteString("GiveawayFulfillmentPlan(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("award_id=")
	builder.WriteString(_m.AwardID)
	builder.WriteString(", ")
	builder.WriteString("interval_rule=")
	builder.WriteString(_m.IntervalRule)
	builder.WriteString(", ")
	builder.WriteString("start_at=")
	builder.WriteString(_m.StartAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("end_at=")
	builder.WriteString(_m.EndAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// GiveawayFulfillmentPlans is a parsable slice of GiveawayFulfillmentPlan.
type GiveawayFulfillmentPlans []*GiveawayFulfillmentPlan
