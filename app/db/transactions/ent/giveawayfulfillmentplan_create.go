// Code generated by ent, DO NOT EDIT.

package ent

import (
	"ItsBagelBot/app/db/transactions/ent/giveawayfulfillmentplan"
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// GiveawayFulfillmentPlanCreate is the builder for creating a GiveawayFulfillmentPlan entity.
type GiveawayFulfillmentPlanCreate struct {
	config
	mutation *GiveawayFulfillmentPlanMutation
	hooks    []Hook
	conflict []sql.ConflictOption
}

// SetAwardID sets the "award_id" field.
func (_c *GiveawayFulfillmentPlanCreate) SetAwardID(v string) *GiveawayFulfillmentPlanCreate {
	_c.mutation.SetAwardID(v)
	return _c
}

// SetIntervalRule sets the "interval_rule" field.
func (_c *GiveawayFulfillmentPlanCreate) SetIntervalRule(v string) *GiveawayFulfillmentPlanCreate {
	_c.mutation.SetIntervalRule(v)
	return _c
}

// SetStartAt sets the "start_at" field.
func (_c *GiveawayFulfillmentPlanCreate) SetStartAt(v time.Time) *GiveawayFulfillmentPlanCreate {
	_c.mutation.SetStartAt(v)
	return _c
}

// SetEndAt sets the "end_at" field.
func (_c *GiveawayFulfillmentPlanCreate) SetEndAt(v time.Time) *GiveawayFulfillmentPlanCreate {
	_c.mutation.SetEndAt(v)
	return _c
}

// SetCreatedAt sets the "created_at" field.
func (_c *GiveawayFulfillmentPlanCreate) SetCreatedAt(v time.Time) *GiveawayFulfillmentPlanCreate {
	_c.mutation.SetCreatedAt(v)
	return _c
}

// SetNillableCreatedAt sets the "created_at" field if the given value is not nil.
func (_c *GiveawayFulfillmentPlanCreate) SetNillableCreatedAt(v *time.Time) *GiveawayFulfillmentPlanCreate {
	if v != nil {
		_c.SetCreatedAt(*v)
	}
	return _c
}

// SetID sets the "id" field.
func (_c *GiveawayFulfillmentPlanCreate) SetID(v string) *GiveawayFulfillmentPlanCreate {
	_c.mutation.SetID(v)
	return _c
}

// Mutation returns the GiveawayFulfillmentPlanMutation object of the builder.
func (_c *GiveawayFulfillmentPlanCreate) Mutation() *GiveawayFulfillmentPlanMutation {
	return _c.mutation
}

// Save creates the GiveawayFulfillmentPlan in the database.
func (_c *GiveawayFulfillmentPlanCreate) Save(ctx context.Context) (*GiveawayFulfillmentPlan, error) {
	_c.defaults()
	return withHooks(ctx, _c.sqlSave, _c.mutation, _c.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (_c *GiveawayFulfillmentPlanCreate) SaveX(ctx context.Context) *GiveawayFulfillmentPlan {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *GiveawayFulfillmentPlanCreate) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *GiveawayFulfillmentPlanCreate) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (_c *GiveawayFulfillmentPlanCreate) defaults() {
	if _, ok := _c.mutation.CreatedAt(); !ok {
		v := giveawayfulfillmentplan.DefaultCreatedAt()
		_c.mutation.SetCreatedAt(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (_c *GiveawayFulfillmentPlanCreate) check() error {
	if _, ok := _c.mutation.AwardID(); !ok {
		return &ValidationError{Name: "award_id", err: errors.New(`ent: missing required field "GiveawayFulfillmentPlan.award_id"`)}
	}
	if v, ok := _c.mutation.AwardID(); ok {
		if err := giveawayfulfillmentplan.AwardIDValidator(v); err != nil {
			return &ValidationError{Name: "award_id", err: fmt.Errorf(`ent: validator failed for field "GiveawayFulfillmentPlan.award_id": %w`, err)}
		}
	}
	if _, ok := _c.mutation.IntervalRule(); !ok {
		return &ValidationError{Name: "interval_rule", err: errors.New(`ent: missing required field "GiveawayFulfillmentPlan.interval_rule"`)}
	}
	if v, ok := _c.mutation.IntervalRule(); ok {
		if err := giveawayfulfillmentplan.IntervalRuleValidator(v); err != nil {
			return &ValidationError{Name: "interval_rule", err: fmt.Errorf(`ent: validator failed for field "GiveawayFulfillmentPlan.interval_rule": %w`, err)}
		}
	}
	if _, ok := _c.mutation.StartAt(); !ok {
		return &ValidationError{Name: "start_at", err: errors.New(`ent: missing required field "GiveawayFulfillmentPlan.start_at"`)}
	}
	if _, ok := _c.mutation.EndAt(); !ok {
		return &ValidationError{Name: "end_at", err: errors.New(`ent: missing required field "GiveawayFulfillmentPlan.end_at"`)}
	}
	if _, ok := _c.mutation.CreatedAt(); !ok {
		return &ValidationError{Name: "created_at", err: errors.New(`ent: missing required field "GiveawayFulfillmentPlan.created_at"`)}
	}
	if v, ok := _c.mutation.ID(); ok {
		if err := giveawayfulfillmentplan.IDValidator(v); err != nil {
			return &ValidationError{Name: "id", err: fmt.Errorf(`ent: validator failed for field "GiveawayFulfillmentPlan.id": %w`, err)}
		}
	}
	return nil
}

func (_c *GiveawayFulfillmentPlanCreate) sqlSave(ctx context.Context) (*GiveawayFulfillmentPlan, error) {
	if err := _c.check(); err != nil {
		return nil, err
	}
	_node, _spec := _c.createSpec()
	if err := sqlgraph.CreateNode(ctx, _c.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != nil {
		if id, ok := _spec.ID.Value.(string); ok {
			_node.ID = id
		} else {
			return nil, fmt.Errorf("unexpected GiveawayFulfillmentPlan.ID type: %T", _spec.ID.Value)
		}
	}
	_c.mutation.id = &_node.ID
	_c.mutation.done = true
	return _node, nil
}

func (_c *GiveawayFulfillmentPlanCreate) createSpec() (*GiveawayFulfillmentPlan, *sqlgraph.CreateSpec) {
	var (
		_node = &GiveawayFulfillmentPlan{config: _c.config}
		_spec = sqlgraph.NewCreateSpec(giveawayfulfillmentplan.Table, sqlgraph.NewFieldSpec(giveawayfulfillmentplan.FieldID, field.TypeString))
	)
	_spec.OnConflict = _c.conflict
	if id, ok := _c.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := _c.mutation.AwardID(); ok {
		_spec.SetField(giveawayfulfillmentplan.FieldAwardID, field.TypeString, value)
		_node.AwardID = value
	}
	if value, ok := _c.mutation.IntervalRule(); ok {
		_spec.SetField(giveawayfulfillmentplan.FieldIntervalRule, field.TypeString, value)
		_node.IntervalRule = value
	}
	if value, ok := _c.mutation.StartAt(); ok {
		_spec.SetField(giveawayfulfillmentplan.FieldStartAt, field.TypeTime, value)
		_node.StartAt = value
	}
	if value, ok := _c.mutation.EndAt(); ok {
		_spec.SetField(giveawayfulfillmentplan.FieldEndAt, field.TypeTime, value)
		_node.EndAt = value
	}
	if value, ok := _c.mutation.CreatedAt(); ok {
		_spec.SetField(giveawayfulfillmentplan.FieldCreatedAt, field.TypeTime, value)
		_node.CreatedAt = value
	}
	return _node, _spec
}

// OnConflict allows configuring the `ON CONFLICT` / `ON DUPLICATE KEY` clause
// of the `INSERT` statement. For example:
//
//	client.GiveawayFulfillmentPlan.Create().
//		SetAwardID(v).
//		OnConflict(
//			// Update the row with the new values
//			// the was proposed for insertion.
//			sql.ResolveWithNewValues(),
//		).
//		// Override some of the fields with custom
//		// update values.
//		Update(func(u *ent.GiveawayFulfillmentPlanUpsert) {
//			SetAwardID(v+v).
//		}).
//		Exec(ctx)
func (_c *GiveawayFulfillmentPlanCreate) OnConflict(opts ...sql.ConflictOption) *GiveawayFulfillmentPlanUpsertOne {
	_c.conflict = opts
	return &GiveawayFulfillmentPlanUpsertOne{
		create: _c,
	}
}

// OnConflictColumns calls `OnConflict` and configures the columns
// as conflict target. Using this option is equivalent to using:
//
//	client.GiveawayFulfillmentPlan.Create().
//		OnConflict(sql.ConflictColumns(columns...)).
//		Exec(ctx)
func (_c *GiveawayFulfillmentPlanCreate) OnConflictColumns(columns ...string) *GiveawayFulfillmentPlanUpsertOne {
	_c.conflict = append(_c.conflict, sql.ConflictColumns(columns...))
	return &GiveawayFulfillmentPlanUpsertOne{
		create: _c,
	}
}

type (
	// GiveawayFulfillmentPlanUpsertOne is the builder for "upsert"-ing
	//  one GiveawayFulfillmentPlan node.
	GiveawayFulfillmentPlanUpsertOne struct {
		create *GiveawayFulfillmentPlanCreate
	}

	// GiveawayFulfillmentPlanUpsert is the "OnConflict" setter.
	GiveawayFulfillmentPlanUpsert struct {
		*sql.UpdateSet
	}
)

// UpdateNewValues updates the mutable fields using the new values that were set on create except the ID field.
// Using this option is equivalent to using:
//
//	client.GiveawayFulfillmentPlan.Create().
//		OnConflict(
//			sql.ResolveWithNewValues(),
//			sql.ResolveWith(func(u *sql.UpdateSet) {
//				u.SetIgnore(giveawayfulfillmentplan.FieldID)
//			}),
//		).
//		Exec(ctx)
func (u *GiveawayFulfillmentPlanUpsertOne) UpdateNewValues() *GiveawayFulfillmentPlanUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithNewValues())
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(s *sql.UpdateSet) {
		if _, exists := u.create.mutation.ID(); exists {
			s.SetIgnore(giveawayfulfillmentplan.FieldID)
		}
		if _, exists := u.create.mutation.AwardID(); exists {
			s.SetIgnore(giveawayfulfillmentplan.FieldAwardID)
		}
		if _, exists := u.create.mutation.IntervalRule(); exists {
			s.SetIgnore(giveawayfulfillmentplan.FieldIntervalRule)
		}
		if _, exists := u.create.mutation.StartAt(); exists {
			s.SetIgnore(giveawayfulfillmentplan.FieldStartAt)
		}
		if _, exists := u.create.mutation.EndAt(); exists {
			s.SetIgnore(giveawayfulfillmentplan.FieldEndAt)
		}
		if _, exists := u.create.mutation.CreatedAt(); exists {
			s.SetIgnore(giveawayfulfillmentplan.FieldCreatedAt)
		}
	}))
	return u
}

// Ignore sets each column to itself in case of conflict.
// Using this option is equivalent to using:
//
//	client.GiveawayFulfillmentPlan.Create().
//	    OnConflict(sql.ResolveWithIgnore()).
//	    Exec(ctx)
func (u *GiveawayFulfillmentPlanUpsertOne) Ignore() *GiveawayFulfillmentPlanUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithIgnore())
	return u
}

// DoNothing configures the conflict_action to `DO NOTHING`.
// Supported only by SQLite and PostgreSQL.
func (u *GiveawayFulfillmentPlanUpsertOne) DoNothing() *GiveawayFulfillmentPlanUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.DoNothing())
	return u
}

// Update allows overriding fields `UPDATE` values. See the GiveawayFulfillmentPlanCreate.OnConflict
// documentation for more info.
func (u *GiveawayFulfillmentPlanUpsertOne) Update(set func(*GiveawayFulfillmentPlanUpsert)) *GiveawayFulfillmentPlanUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(update *sql.UpdateSet) {
		set(&GiveawayFulfillmentPlanUpsert{UpdateSet: update})
	}))
	return u
}

// Exec executes the query.
func (u *GiveawayFulfillmentPlanUpsertOne) Exec(ctx context.Context) error {
	if len(u.create.conflict) == 0 {
		return errors.New("ent: missing options for GiveawayFulfillmentPlanCreate.OnConflict")
	}
	return u.create.Exec(ctx)
}

// ExecX is like Exec, but panics if an error occurs.
func (u *GiveawayFulfillmentPlanUpsertOne) ExecX(ctx context.Context) {
	if err := u.create.Exec(ctx); err != nil {
		panic(err)
	}
}

// Exec executes the UPSERT query and returns the inserted/updated ID.
func (u *GiveawayFulfillmentPlanUpsertOne) ID(ctx context.Context) (id string, err error) {
	if u.create.driver.Dialect() == dialect.MySQL {
		// In case of "ON CONFLICT", there is no way to get back non-numeric ID
		// fields from the database since MySQL does not support the RETURNING clause.
		return id, errors.New("ent: GiveawayFulfillmentPlanUpsertOne.ID is not supported by MySQL driver. Use GiveawayFulfillmentPlanUpsertOne.Exec instead")
	}
	node, err := u.create.Save(ctx)
	if err != nil {
		return id, err
	}
	return node.ID, nil
}

// IDX is like ID, but panics if an error occurs.
func (u *GiveawayFulfillmentPlanUpsertOne) IDX(ctx context.Context) string {
	id, err := u.ID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// GiveawayFulfillmentPlanCreateBulk is the builder for creating many GiveawayFulfillmentPlan entities in bulk.
type GiveawayFulfillmentPlanCreateBulk struct {
	config
	err      error
	builders []*GiveawayFulfillmentPlanCreate
	conflict []sql.ConflictOption
}

// Save creates the GiveawayFulfillmentPlan entities in the database.
func (_c *GiveawayFulfillmentPlanCreateBulk) Save(ctx context.Context) ([]*GiveawayFulfillmentPlan, error) {
	if _c.err != nil {
		return nil, _c.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(_c.builders))
	nodes := make([]*GiveawayFulfillmentPlan, len(_c.builders))
	mutators := make([]Mutator, len(_c.builders))
	for i := range _c.builders {
		func(i int, root context.Context) {
			builder := _c.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*GiveawayFulfillmentPlanMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, _c.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					spec.OnConflict = _c.conflict
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, _c.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, _c.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (_c *GiveawayFulfillmentPlanCreateBulk) SaveX(ctx context.Context) []*GiveawayFulfillmentPlan {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *GiveawayFulfillmentPlanCreateBulk) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *GiveawayFulfillmentPlanCreateBulk) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}

// OnConflict allows configuring the `ON CONFLICT` / `ON DUPLICATE KEY` clause
// of the `INSERT` statement. For example:
//
//	client.GiveawayFulfillmentPlan.CreateBulk(builders...).
//		OnConflict(
//			// Update the row with the new values
//			// the was proposed for insertion.
//			sql.ResolveWithNewValues(),
//		).
//		// Override some of the fields with custom
//		// update values.
//		Update(func(u *ent.GiveawayFulfillmentPlanUpsert) {
//			SetAwardID(v+v).
//		}).
//		Exec(ctx)
func (_c *GiveawayFulfillmentPlanCreateBulk) OnConflict(opts ...sql.ConflictOption) *GiveawayFulfillmentPlanUpsertBulk {
	_c.conflict = opts
	return &GiveawayFulfillmentPlanUpsertBulk{
		create: _c,
	}
}

// OnConflictColumns calls `OnConflict` and configures the columns
// as conflict target. Using this option is equivalent to using:
//
//	client.GiveawayFulfillmentPlan.Create().
//		OnConflict(sql.ConflictColumns(columns...)).
//		Exec(ctx)
func (_c *GiveawayFulfillmentPlanCreateBulk) OnConflictColumns(columns ...string) *GiveawayFulfillmentPlanUpsertBulk {
	_c.conflict = append(_c.conflict, sql.ConflictColumns(columns...))
	return &GiveawayFulfillmentPlanUpsertBulk{
		create: _c,
	}
}

// GiveawayFulfillmentPlanUpsertBulk is the builder for "upsert"-ing
// a bulk of GiveawayFulfillmentPlan nodes.
type GiveawayFulfillmentPlanUpsertBulk struct {
	create *GiveawayFulfillmentPlanCreateBulk
}

// UpdateNewValues updates the mutable fields using the new values that
// were set on create. Using this option is equivalent to using:
//
//	client.GiveawayFulfillmentPlan.Create().
//		OnConflict(
//			sql.ResolveWithNewValues(),
//			sql.ResolveWith(func(u *sql.UpdateSet) {
//				u.SetIgnore(giveawayfulfillmentplan.FieldID)
//			}),
//		).
//		Exec(ctx)
func (u *GiveawayFulfillmentPlanUpsertBulk) UpdateNewValues() *GiveawayFulfillmentPlanUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithNewValues())
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(s *sql.UpdateSet) {
		for _, b := range u.create.builders {
			if _, exists := b.mutation.ID(); exists {
				s.SetIgnore(giveawayfulfillmentplan.FieldID)
			}
			if _, exists := b.mutation.AwardID(); exists {
				s.SetIgnore(giveawayfulfillmentplan.FieldAwardID)
			}
			if _, exists := b.mutation.IntervalRule(); exists {
				s.SetIgnore(giveawayfulfillmentplan.FieldIntervalRule)
			}
			if _, exists := b.mutation.StartAt(); exists {
				s.SetIgnore(giveawayfulfillmentplan.FieldStartAt)
			}
			if _, exists := b.mutation.EndAt(); exists {
				s.SetIgnore(giveawayfulfillmentplan.FieldEndAt)
			}
			if _, exists := b.mutation.CreatedAt(); exists {
				s.SetIgnore(giveawayfulfillmentplan.FieldCreatedAt)
			}
		}
	}))
	return u
}

// Ignore sets each column to itself in case of conflict.
// Using this option is equivalent to using:
//
//	client.GiveawayFulfillmentPlan.Create().
//		OnConflict(sql.ResolveWithIgnore()).
//		Exec(ctx)
func (u *GiveawayFulfillmentPlanUpsertBulk) Ignore() *GiveawayFulfillmentPlanUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithIgnore())
	return u
}

// DoNothing configures the conflict_action to `DO NOTHING`.
// Supported only by SQLite and PostgreSQL.
func (u *GiveawayFulfillmentPlanUpsertBulk) DoNothing() *GiveawayFulfillmentPlanUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.DoNothing())
	return u
}

// Update allows overriding fields `UPDATE` values. See the GiveawayFulfillmentPlanCreateBulk.OnConflict
// documentation for more info.
func (u *GiveawayFulfillmentPlanUpsertBulk) Update(set func(*GiveawayFulfillmentPlanUpsert)) *GiveawayFulfillmentPlanUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(update *sql.UpdateSet) {
		set(&GiveawayFulfillmentPlanUpsert{UpdateSet: update})
	}))
	return u
}

// Exec executes the query.
func (u *GiveawayFulfillmentPlanUpsertBulk) Exec(ctx context.Context) error {
	if u.create.err != nil {
		return u.create.err
	}
	for i, b := range u.create.builders {
		if len(b.conflict) != 0 {
			return fmt.Errorf("ent: OnConflict was set for builder %d. Set it on the GiveawayFulfillmentPlanCreateBulk instead", i)
		}
	}
	if len(u.create.conflict) == 0 {
		return errors.New("ent: missing options for GiveawayFulfillmentPlanCreateBulk.OnConflict")
	}
	return u.create.Exec(ctx)
}

// ExecX is like Exec, but panics if an error occurs.
func (u *GiveawayFulfillmentPlanUpsertBulk) ExecX(ctx context.Context) {
	if err := u.create.Exec(ctx); err != nil {
		panic(err)
	}
}
