# Copyright (c) 2026 Adam Ousmer. All rights reserved.
# Proprietary. No license granted. See LICENSE.md.

FROM docker.io/library/elixir:1.20-otp-28@sha256:e0ea48e598d926c46ac4f03d7025e73bfa132b887469bc2d79196849a5e01f61 AS build

ENV MIX_ENV=prod
WORKDIR /src

RUN mix local.hex --force && mix local.rebar --force

COPY mix.exs mix.lock* ./
RUN mix deps.get --only prod

COPY config config
RUN mix deps.compile

COPY lib lib
RUN mix compile && mix release

# Must match the builder's base distro: the release links against its glibc.
FROM docker.io/library/debian:trixie-slim@sha256:a99cfc517144bc59b1978475ec53b46ecabec7e43635402ee5b77cc54cd1b20a

RUN apt-get update \
 && apt-get install -y --no-install-recommends libssl3 libncurses6 ca-certificates \
 && rm -rf /var/lib/apt/lists/* \
 && useradd --system --home /app --shell /usr/sbin/nologin ingress

COPY --from=build --chown=ingress:ingress /src/_build/prod/rel/ingress /app

USER ingress
WORKDIR /app

ENV LANG=C.UTF-8
ENTRYPOINT ["/app/bin/ingress"]
CMD ["start"]
