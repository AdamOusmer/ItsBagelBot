---
// Copyright (c) 2026 Adam Ousmer. All rights reserved.
// Proprietary. No license granted. See LICENSE.md.

// Compact locale cluster for the public nav (`.bb-lang-switch` in
// ../styles/elements/nav.css), not the page-filter rail. Its Svelte twin is
// ../svelte/LanguageSwitcher.svelte and ../test/parity.test.ts diffs the two,
// so the class order (`bb-lang-switch__opt is-active`) and the
// attribute order here match that file.
import '../styles/elements/nav.css';
import type { UiLocaleOption } from '../lib/nav-types';

interface Props {
  options: UiLocaleOption[];
  ariaLabel: string;
  class?: string;
  [key: string]: unknown;
}

const { options, ariaLabel, class: className = '', ...rest } = Astro.props;

const classes = ['bb-lang-switch', className || null]
  .filter(Boolean)
  .join(' ');
---

<div class={classes} role="group" aria-label={ariaLabel} {...rest}>{options.map((option) => (
  <a
    class={`bb-lang-switch__opt ${option.current ? 'is-active' : ''}`}
    href={option.href}
    hreflang={option.code}
    aria-current={option.current ? 'true' : undefined}
  >{option.label}</a>
))}</div>
